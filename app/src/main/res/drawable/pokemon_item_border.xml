<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Border -->
    <stroke
        android:width="2dp"
        android:color="#2196F3" /> <!-- plava boja -->

    <!-- Pozadina -->
    <solid android:color="@android:color/transparent" />

    <!-- Zaobljeni uglovi -->
    <corners android:radius="8dp" />
</shape>
